export enum GameEndStatus {
  won = 'won',
  lost = 'lost',
}

export enum GameStatus {
  win = 'win',
  lose = 'lose',
  disabled = 'disabled',
  active = 'active',
}

export enum MinesCounter {
  One = 1,
  Three = 3,
  Five = 5,
  Ten = 10,
  Fifteen = 15,
}